import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "login page",
  description: "login page description",
};

export default function DispatchPage() {
  return <h1>DispatchPage</h1>;
}
